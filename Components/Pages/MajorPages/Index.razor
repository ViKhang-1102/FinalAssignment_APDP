@page "/majors"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using FinalAssignemnt_APDP.Data
@using FinalAssignemnt_APDP.Data
@implements IAsyncDisposable
@inject IDbContextFactory<FinalAssignemnt_APDP.Data.ApplicationDbContext> DbFactory

<PageTitle>Major Management</PageTitle>

<div class="container py-5 fade-in">
    <div class="d-flex align-items-center justify-content-between mb-4">
        <div>
            <h2 class="fw-bold text-primary mb-1">
                <i class="bi bi-mortarboard-fill me-2"></i>Major List
            </h2>
            <p class="text-muted mb-0">Manage academic majors and departments</p>
        </div>
        <div>
            <a href="majors/create" class="btn btn-gradient-primary px-4 py-2 rounded-pill d-inline-flex align-items-center">
                <i class="bi bi-plus-lg me-2"></i> Create Major
            </a>
        </div>
    </div>

    <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
        <div class="card-body p-0">

            <QuickGrid Class="table table-borderless custom-table mb-0 align-middle" Items="DbFactory.CreateDbContext().Majors.Include(m => m.Department)">

                <TemplateColumn Title="Major Name" Sortable="true" Class="ps-4">
                    <div class="d-flex align-items-center py-2">
                        <div class="bg-light text-warning rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                            <i class="bi bi-diagram-3-fill"></i>
                        </div>
                        <span class="fw-bold text-dark fs-6">@context.Name</span>
                    </div>
                </TemplateColumn>

                <TemplateColumn Title="Department" Sortable="true">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-soft-primary text-primary border border-primary border-opacity-25 rounded-pill px-3 py-2">
                            <i class="bi bi-building me-1"></i> @context.Department?.Name
                        </span>
                    </div>
                </TemplateColumn>

                <TemplateColumn Title="Actions" Class="text-end pe-4">
                    <div class="btn-group shadow-sm rounded-3" role="group">
                        <a href="@($"majors/edit?id={context.Id}")" class="btn btn-light btn-sm text-primary" title="Edit">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a href="@($"majors/details?id={context.Id}")" class="btn btn-light btn-sm text-info" title="Details">
                            <i class="bi bi-info-circle"></i>
                        </a>
                        <a href="@($"majors/delete?id={context.Id}")" class="btn btn-light btn-sm text-danger" title="Delete">
                            <i class="bi bi-trash"></i>
                        </a>
                    </div>
                </TemplateColumn>

            </QuickGrid>
        </div>

        <div class="card-footer bg-white border-top border-light py-3 px-4">
            <small class="text-muted">
                <i class="bi bi-layers-fill me-1"></i> Academic Structure
            </small>
        </div>
    </div>
</div>

@code {
    private ApplicationDbContext context = default!;

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}
