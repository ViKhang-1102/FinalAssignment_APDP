@page "/majors"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using FinalAssignemnt_APDP.Data
@using FinalAssignemnt_APDP.Constants
@implements IAsyncDisposable
@inject IDbContextFactory<FinalAssignemnt_APDP.Data.ApplicationDbContext> DbFactory

<PageTitle>Majors</PageTitle>

<div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-6 space-y-6">
    <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
        <div>
            <p class="text-xs uppercase tracking-widest text-slate-500">Programs</p>
            <h1 class="text-3xl font-bold text-slate-900">Majors</h1>
            <p class="text-sm text-slate-500">Align program structures with departments.</p>
        </div>
        <a href="@AppRoutes.MajorsCreate" class="inline-flex items-center gap-2 rounded-2xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow transition hover:-translate-y-0.5 hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            <i class="bi bi-plus-circle"></i> New Major
        </a>
    </div>

    <div class="rounded-3xl border border-slate-200 bg-white shadow-sm">
        <div class="overflow-x-auto">
            <QuickGrid Class="min-w-full divide-y divide-slate-200 text-sm text-slate-600" Items="context.Majors">
                <PropertyColumn Title="Name" Property="major => major.Name" />
                <PropertyColumn Title="Department" Property="major => major.DepartmentId" />

                <TemplateColumn Title="Actions" Context="major">
                    <div class="flex flex-wrap items-center justify-end gap-2">
                        <a class="@GetActionClass("indigo")" href="@AppRoutes.MajorsEdit(major.Id)"><i class="bi bi-pencil"></i> Edit</a>
                        <a class="@GetActionClass("sky")" href="@AppRoutes.MajorsDetails(major.Id)"><i class="bi bi-info-circle"></i> Detail</a>
                        <a class="@GetActionClass("rose")" href="@AppRoutes.MajorsDelete(major.Id)"><i class="bi bi-trash"></i> Delete</a>
                    </div>
                </TemplateColumn>
            </QuickGrid>
        </div>
    </div>
</div>

@code {
    private ApplicationDbContext context = default!;
    private const string ActionBase = "inline-flex items-center gap-1.5 rounded-full border px-3 py-1.5 text-xs font-semibold transition hover:-translate-y-0.5 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2";

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();

    private string GetActionClass(string tone) => tone switch
    {
        "sky" => $"{ActionBase} border-sky-100 text-sky-600 hover:bg-sky-50 focus-visible:outline-sky-500",
        "rose" => $"{ActionBase} border-rose-100 text-rose-600 hover:bg-rose-50 focus-visible:outline-rose-500",
        _ => $"{ActionBase} border-indigo-100 text-indigo-600 hover:bg-indigo-50 focus-visible:outline-indigo-500"
    };
}
