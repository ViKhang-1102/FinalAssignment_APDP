@inject NavigationManager NavigationManager
@using FinalAssignemnt_APDP.Constants

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(
            AppRoutes.LoginWithReturnUrl(NavigationManager.Uri), 
            forceLoad: true);
    }
}
